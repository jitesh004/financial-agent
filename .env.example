# Optional. Without it the app still parses, reconciles, categorizes by rules,
# and computes every figure - you lose only the written narrative and the
# model-assisted categorization of unrecognised merchants.
ANTHROPIC_API_KEY=sk-ant-...
